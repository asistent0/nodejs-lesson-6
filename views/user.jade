//
   Created by asistent on 18.05.2016.


doctype html
html(lang="en")
    head
        meta(charset='utf8')
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1")
        title ToDo List
        link(rel="stylesheet", href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css")
        link(rel="stylesheet", href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap-theme.min.css")
        <!--[if lt IE 9]>
        script(src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js")
        script(src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js")
        <![endif]-->
        style.
            .user {
                margin: 10px;
                padding: 10px;
            }
body
    div.container
        div.page-header
            div.row
                div.col-md-9
                    h1.
                        Привет
                div.col-md-3
                    span.user
                        a.btn.btn-info(href="/") На главную
                    span.user
                        a.btn.btn-warning(href="/logout") Выход
        div.content
            p Тут будет вся информация о пользователе
            p Для перехода к списку todo листа нажмите кнопку <b> На главную </b>
            p Для выхода из приложения можно нажать кнопку <b>Выход</b>

    script(src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js")
    script(src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js")
    script.
        $(document).ready(function () {
            $.post('/user', function (data) {
                $('div.col-md-9 > h1').append(' <em>' + data.username + '</em>');
            })
        })